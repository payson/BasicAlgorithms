The Algorithms is a list of algorithms that are "Basic" algorithms to Payson.

(ordered by added time)
1. Serialization.java contains two ways to serialize/deserialize a binary tree.
	One way is considered as BFS 
	The other way is considered as Preorder
